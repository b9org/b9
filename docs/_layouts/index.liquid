<!doctype html>
<html lang="{{ page.lang | default: site.lang | default: "en" }}">
  {%- include head.liquid -%}
  <body>
  <title>Base9 Home</title>
  <main class="page-content" aria-label="Content">
      {%- include navbar.liquid -%}
      {%- include logo.liquid -%}
      <div class="wrapper">
        {{ content }}
      </div>
      {%- include footer.liquid -%}
    </main>
  </body>
</html>
