
function b9main() {
    var a = 100;
    var b = 20;
    b9PrintString("a + b =");
    b9PrintNumber(a + b);

    return fib(12);
}

// main, no args for b9 entry point
function fib(arg) {
    if (3 > arg)
    return 1;
    return fib(arg - 1) + fib(arg - 2);
}

