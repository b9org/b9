function factorial(a) {
  if (a == 0) return 1;
  // TODO: Implement multiply
  // return a * factorial(a - 1);
  return 0;
}

function b9main() {
    b9PrintString("");
    b9PrintString("Factorial");
    var a = 1;
    while (a <= 20) {
        b9PrintNumber(factorial(a));
        a++;
    }
}

